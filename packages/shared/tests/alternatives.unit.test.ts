// ドメイン: 代替案選定は常に3件（フォールバック含む）
// 実装は `packages/api/src/rules` または共有関数を想定してRedから開始。

import { describe, test } from 'vitest';

describe('buildAlternatives', () => {
  test.todo('always returns 3 suggestions based on amountIncl, tone, and category');
});

